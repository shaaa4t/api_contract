name: api_contract
description: Runtime API response contract validator for Flutter/Dart. Detect API mismatches before they crash your app. Works with any HTTP client.
version: 0.2.0
homepage: https://github.com/ahmedshaat/api_contract
repository: https://github.com/ahmedshaat/api_contract

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  meta: ^1.16.0

dev_dependencies:
  dio: ^5.9.1
  http: ^1.2.0
  test: ^1.29.0
  lints: ^6.1.0
  build_runner: ^2.4.13
